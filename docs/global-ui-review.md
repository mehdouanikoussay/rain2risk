# Global UI review

The live page title is `Rain2Risk Global GeoAI MVP`.

The map opens on a world view rather than a Tunisia-only view. The UI shows the selected coordinates, My location, Analyze this location, the data sources panel, the Leaflet layer control, and the risk legend.

The browser screenshot was captured from the live application at the temporary public URL. A browser image-element save may capture only a map tile, so the full browser screenshot is copied separately from the browser screenshot output when packaging.
